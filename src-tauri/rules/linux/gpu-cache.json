{
  "$schema": "../schema/rules.schema.json",
  "type": "gpu-cache",
  "apps": [
    { "id": "mesa-cache", "name": "Mesa Shader Cache", "paths": ["${CACHE}/mesa_shader_cache"], "description": "Mesa/OpenGL compiled shader cache; rebuilt automatically per-application" },
    { "id": "nvidia-cache", "name": "NVIDIA Shader Cache", "paths": ["${CACHE}/nvidia/GLCache"], "description": "NVIDIA compiled shader cache for OpenGL and Vulkan; rebuilt automatically per-game" },
    { "id": "unity-cache", "name": "Unity Shader Cache", "paths": ["${CACHE}/unity3d"], "description": "Unity engine compiled shader cache; rebuilt when Unity projects are opened" }
  ]
}
